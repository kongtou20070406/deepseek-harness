# 科研辅助 Harness：扩展需求与长期愿景 v0.1

> 本文已不再作为第一阶段开发的核心规格。精简后的权威需求见 [`HARNESS_REQUIREMENTS.md`](HARNESS_REQUIREMENTS.md)。本文保留用于记录未来可能扩展的科研状态、证据和资源管理能力。

> 状态：讨论稿
> 日期：2026-08-11
> 本文只定义“为什么做、必须解决什么、怎样算初步成功”。它不预设 PI、Codex App Server、具体数据库、Agent 数量或前端技术栈。

## 0. 零号要求：上下文机制不能改变或遗忘核心 Idea

本项目首先要解决的问题不是“怎样让 Agent 更自治”，而是：

> **无论对话经历多少轮、发生多少次摘要、压缩、检索或模型切换，研究者的核心 Idea 都不能被静默改写、摘要替代或从有效上下文中丢失。**

因此，核心 Idea 不能只存在于聊天历史、rolling summary、模型记忆或向量检索结果里。它必须是上下文系统之外的独立权威状态。

设当前经研究者确认的核心 Idea 为 `I_v`，普通工作历史为 `H_t`，当前任务为 `q_t`，动态检索材料为 `R_t`，则上下文应满足：

```text
C_t = I_v ⊕ TaskContext(q_t) ⊕ Retrieve(H_t, R_t)
```

其中：

- `I_v` 是确定性注入的必选上下文，不依赖检索是否命中；
- 压缩只允许作用于 `H_t` 等工作上下文，不能作用于 `I_v`；
- Context Compiler 对 `I_v` 只有读取权，没有写入权；
- Agent 对 Idea 的不同理解只能作为解释或变更提案，不能覆盖 `I_v`；
- Idea 只有经过研究者显式确认，才产生新版本 `I_(v+1)`；
- 旧版本永久保留，不能被新摘要覆盖。

如果 token 预算不足，系统应依次丢弃低价值动态材料、压缩工作历史或拆分任务，不能通过擅自压缩核心 Idea 来满足预算。

如果核心 Idea 本身过长，可以同时维护“完整原文”和“经研究者确认的 Idea Kernel”；但自动生成的摘要在获得确认前不能成为权威版本，也不能代替完整原文。

## 1. 一句话定义

这是一个以 **核心 Idea 不被上下文机制损坏** 为首要约束的轻量科研辅助系统：它把研究者确认的 Idea 与证据、任务和工作区状态组装成恰当的任务上下文，驱动 Agent 完成可执行的科研工作，并把结果以可审查的方式反馈给研究者。

系统的基本闭环是：

```text
Idea(t)
  → 选择下一项科研任务
  → 组装该任务所需的最小充分上下文
  → Agent 执行并产生过程、产物和观察
  → 人判断其科学意义
  → Idea(t+1)
```

系统的最高目标不是自主写论文，也不是尽量减少人的点击次数，而是：

> 在不削弱研究者认知控制的前提下，减少上下文整理、重复执行和进度追踪等机械负担，加快“想法—行动—证据—理解”的循环。

## 2. 当前真正要解决的三个核心问题

### 2.1 Idea 如何独立于对话被长期保存

聊天记录、论文草稿和代码仓库都不能完整代表一个科研 Idea。系统需要保存：

- 研究者最初及后续的原始表达；
- 系统对问题、假设、机制、预测和未知项的结构化理解；
- 哪些观察或决定导致了 Idea 的变化；
- 当前仍存在的冲突、分歧和未决问题。

原始表达与结构化视图必须共存。结构化视图是可修订的投影，不能反过来替代研究者的原始表达。

Idea 的改变与上下文压缩是两个完全不同的操作：压缩不能触发 Idea 版本变化，Idea 版本变化也不能由模型自行提交。

### 2.2 如何为当前任务组装正确上下文

Agent 不应默认读取全部历史，也不能只依赖最近对话或一次滚动摘要。系统应依据当前任务，从 Idea 状态、研究记录和工作区中生成一个可检查的 `Context Pack`。

上下文方案的优化顺序是不可交换的：先最大化公开基准上的任务成功率，任何明显掉正确率的省 token 方案直接淘汰；只在正确率统计等价的方案中最小化注入上下文；再在前两项相当时最小化本地组装延迟及 P95。不得把三者合成加权总分。

Context Pack 必须回答：

1. 这次任务要解决什么？
2. 哪些信息必须出现，遗漏就可能改变任务含义？
3. 哪些历史材料是按相关性动态加入的？
4. 每条重要信息来自哪里、当前可信度如何？
5. 哪些相关信息因预算或判断没有加入？
6. 本次确定性注入的是哪个 Idea 版本，内容是否与权威状态一致？

“最小充分”目前是待验证目标，不是假定已经有正确算法。

### 2.3 Agent 工作流如何真正推动科研，而不是制造管理负担

研究者只面对一个主要交互入口。系统内部可以规划、调用工具或临时并行执行，但研究者不需要管理一组固定人格的 Agent。

工作流必须围绕可观察的科研状态推进，而不是围绕 Agent 之间的对话推进：

```text
任务提出 → 上下文编译 → 执行 → 观察 → 产物 → 状态变更提案 → 人的裁决
```

Agent 的输出重点应是任务进度、产物、观察、失败原因、证据关系和建议变更，而不是不可复用的长篇聊天。

## 3. 核心产品原则

### P1. Idea 是中心，Agent 是可替换执行者

系统的长期资产是 Idea 的轨迹及其证据链，而不是某次会话、某个模型或某个 Agent 角色。

核心 Idea 必须由独立状态存储提供，不能通过恢复聊天记录或读取最近摘要间接重建。

### P2. 辅助优先于自治

系统应主动承担可执行工作，但不应替研究者静默改变核心问题、主要主张或科学信念。

### P3. 人保留科学意义的裁决权

Agent 可以挑战 Idea、提出修订并展示依据；核心 Idea 的正式变更必须显式呈现给人，并保留接受、拒绝、修改或暂缓的空间。

### P4. 给模型正确状态，而不是全部历史

上下文需要按任务确定性地纳入关键约束，再按需检索补充材料。检索不能决定核心主张是否存在。

对话压缩器不得读写权威 Idea；Context Compiler 每次组装时必须显式记录注入的 Idea 版本与内容哈希。

### P5. 执行和证据分层

允许快速试探、失败和调试；但一次 Run 或一个指标不能自动升级为可信证据，更不能自动改变核心 Idea。

### P6. 可确定的约束尽量程序化

文件存在、进程状态、配置一致性、运行完成度、数值异常和版本关系等，应优先由代码检查，而不是再调用一个 Agent 发表意见。

### P7. 过程必须可见、可暂停、可恢复

研究者需要知道系统正在做什么、为什么做、卡在哪里，并能从中断处继续，而不是面对一个长期无反馈的黑箱任务。

### P8. 轻量与可替换

首个版本优先采用本地单用户、少依赖、单体后端。模型运行时、上下文策略、状态存储和 UI 之间应有清晰边界，避免把产品语义绑定到某一个 Harness。

## 4. 最小领域模型

第一版只要求以下概念，不要求立即使用图数据库或复杂本体：

### Idea

- 原始表达；
- 当前结构化理解；
- 版本与分支关系；
- 当前重要未知项；
- 待研究者裁决的变更提案。

### Task

- 本次要回答的问题或要完成的动作；
- 与 Idea 的关系；
- 状态、优先级和停止条件；
- 资源与权限边界。

### Context Pack

- 任务说明；
- 必选上下文；
- 动态检索上下文；
- 来源与选择理由；
- 上下文版本或内容哈希。

### Run

- 一次实际执行；
- 配置、环境、日志、状态和产物；
- 允许失败、取消或仅用于调试。

### Observation

- 从 Run、文献、代码或人工输入中得到的观察；
- 来源、时间和适用条件；
- 尚不等价于 Evidence。

### Evidence Link

- 某个观察支持、反驳或限制哪个假设/主张；
- 由谁确认、依据是什么；
- 可保留“不确定”而不强制二元判断。

### Decision / Proposed Delta

- Agent 建议怎样改变任务、假设或 Idea；
- 人最终接受、修改、拒绝或暂缓了什么；
- 决定发生时可见的上下文与依据。

### Artifact

- 代码、配置、数据、图表、报告、模型权重或其他可复用成果；
- 与产生它的 Run 和支持的 Observation 建立关系。

## 5. 必须具备的用户体验

前端不是聊天框的装饰，而是科研状态的可视化入口。第一版至少需要四个视图：

1. **对话与控制**：提出 Idea、下达任务、处理 Agent 的问题和审批；
2. **Idea 演化**：查看当前 Idea、历史版本、变更原因和待裁决提案；
3. **实验/任务进度**：查看正在运行、已完成、失败、暂停的任务及实时事件；
4. **证据与产物**：从假设或主张追溯到 Observation、Run、配置和 Artifact。

任何一次核心 Idea 变更都应该能回答：

> 改了什么？为什么改？依据是什么？是谁确认的？能否回到之前的版本？

## 6. Agent Workflow 的最低行为要求

每个工作流实例必须：

1. 接收一个明确 Task；
2. 生成并保存可检查的 Context Pack；
3. 在权限范围内执行工具或命令；
4. 持续发出结构化进度事件；
5. 保存 Run、日志和 Artifact 的引用；
6. 区分事实观察、解释、猜测与建议；
7. 以 `Proposed Delta` 提交对长期状态的修改；
8. 在需要科学判断或新增权限时交还给人；
9. 支持取消、失败恢复和重新运行；
10. 不因一次摘要、压缩或模型重启丢失任务状态。

第一版不要求固定 Planner、Researcher、Coder、Reviewer 等角色。只有当临时 Worker 能降低关键路径时间或隔离上下文时，才值得引入。

## 7. MVP：一个完整的纵向闭环

首个原型只验证一条路径：

1. 研究者输入一个真实但规模受控的 Idea；
2. 系统同时保存原始表达并生成可编辑的结构化视图；
3. 研究者从 Idea 创建一项可执行任务；
4. 系统生成可查看的 Context Pack；
5. Agent 在本地工作区执行一个小型代码或实验任务；
6. 前端实时显示步骤、日志、错误和产物；
7. 系统从结果生成 Observation 与 Proposed Delta；
8. 研究者裁决是否更新 Idea；
9. 重启系统后，仍能恢复完整状态并解释本次变化。

这个闭环跑通之前，不扩展为自动论文生成、通用文献平台、复杂多 Agent 实验室或分布式 GPU 调度器。

## 8. MVP 验收标准

### 功能正确性

- Idea 原始表达不会被系统生成内容覆盖；
- 连续执行多次上下文压缩后，权威 Idea 的内容与哈希保持不变；
- 新会话、模型切换和进程重启后，Context Pack 仍确定性包含同一 Idea 版本；
- 即使动态检索完全失败，核心 Idea 仍然进入上下文；
- Agent 不能直接写入权威 Idea，只能创建待裁决的 Proposed Delta；
- 每次 Idea 正式变更都有版本、依据和人工裁决记录；
- 每次 Agent 执行都能追溯到 Task 与 Context Pack；
- Run、Observation、Evidence Link 和 Idea 更新互不混淆；
- 任务可取消，进程异常后可恢复或明确标记失败；
- 前端能完整展示第 7 节的纵向闭环。

### 产品价值验证

在 3–5 个真实科研小任务上，记录并比较：

- 研究者为 Agent 补充遗漏上下文的次数；
- Agent 因错误上下文产生的返工次数；
- 从提出任务到获得可判断结果的时间；
- 研究者是否能在不阅读完整聊天记录的情况下解释 Idea 的变化；
- 系统管理本身消耗的时间是否低于它节省的时间。

这些指标用于验证 Context Pack 与 Workflow 是否有效，而不是证明 AI 已经“自动完成科研”。

## 9. 当前非目标

- 自动发现“真正重要”的科研问题；
- 端到端自动生成论文；
- 让 AI 独立决定研究方向或科学信念；
- 预设 Luna/Sol 或其他固定 Agent 组织结构；
- 构建通用 AutoResearch 平台；
- 一开始支持多人协作、云端多租户或复杂权限系统；
- 一开始构建知识图谱、向量数据库集群或分布式调度；
- 追求最大 GPU 利用率、最大并发 Agent 数或最大自动化步骤数；
- 取代已有 IDE、实验追踪器或文献管理器的全部功能。

## 10. 可行性判断与技术边界

### 已知可行

- PI 提供可嵌入 Node.js 应用的 SDK，以及适合前后端分离的 RPC/事件接口；
- PI 当前支持通过 OAuth 使用 ChatGPT Plus/Pro 的 OpenAI Codex 订阅；
- OpenAI Codex 也提供 App Server，可向自定义富客户端暴露认证、会话、审批和流式事件；
- 因此前端、后端、Agent runtime 分离的本地原型不存在明显技术障碍。

### 仍需用 Spike 验证

- PI 的 SDK/RPC 在 ChatGPT 订阅 OAuth 下是否能稳定支持我们的长任务、取消、恢复与并发需求；
- 订阅的使用限制、模型可用性和行为变化是否适合持续实验执行；
- PI 的事件与会话模型能否自然映射到 Task / Run / Context Pack，而不迫使产品围绕聊天会话设计；
- Windows 上进程管理、终止子进程、日志流和环境隔离是否可靠；
- 如何隔离 Agent 对文件、网络、凭据和实验资源的访问。PI 默认继承启动它的用户权限，不能把“能运行”误当作“已安全”。

### 当前技术立场

PI 是优先验证的候选 runtime，不是产品核心。应通过一个很薄的 `AgentRuntime` 适配层接入，使未来可以比较或替换为 Codex App Server、Codex SDK、API 模型或本地模型，而无需重写 Idea、Context 和 Workflow 语义。

## 11. 尚未决定的问题

这些问题会实质改变设计，需要通过讨论或 Spike 决定：

1. 一个项目只有一个主 Idea，还是允许多个竞争 Idea/分支并存？
2. 哪些字段属于必须人工确认的“核心状态”，哪些可以由 Agent 自动维护？
3. 首个真实纵向用例是什么：复现实验、修改模型并做小规模验证，还是分析已有实验异常？
4. 第一版只运行本机命令，还是必须从一开始支持 SSH 到实验服务器？
5. Context Pack 的“好坏”如何做可重复评估，而不只靠主观感觉？
6. 前端是独立桌面应用、浏览器本地 Web UI，还是先用极简开发界面验证闭环？
7. 订阅额度达到限制时，任务应该暂停、降级模型，还是允许切换 API key/本地模型？

## 12. 下一步建议

在确定完整技术方案前，先做两个短 Spike：

### Spike A：Runtime 可行性

用 PI + ChatGPT 订阅完成一次可取消、可流式观察、能保存产物的本地任务，并验证重启恢复。此 Spike 只验证运行时，不设计完整产品。

### Spike B：科研闭环可行性

用最简单的存储和界面手工实现一个 Idea → Context Pack → Run → Observation → Proposed Delta → 人工确认 → Idea 新版本的闭环。此 Spike 甚至可以暂时不用复杂 Agent，以验证领域模型是否自然。

如果二者都成立，再决定正式技术栈与模块边界。
