# Pi Idea Extension 能力结果规格

> 本文只约束扩展增强 Pi 后必须产生的效果和可验证结果，不指定界面布局、进程结构、存储方式、模型编排、上下文算法或实现技术。
>
> 旧 V0 源码不得复制、导入、重构或成为新版依赖。新版必须在旧 archive 完全不可用时仍能独立工作。

## 一、扩展能力目标

Pi Idea Extension 是 Pi 的可选扩展。使用主体始终是 Pi；扩展只为普通 Pi 对话增加按需启用的长期科研能力，不建立独立产品、专门入口或新的对话类型。

每一段 Pi 对话最初都是普通对话。用户可以在任意时刻为当前对话启用 Idea 保持，使它在不更换窗口、不迁移 session、不重新开始对话的情况下，获得数周或数月持续工作的潜能。用户也可以始终不用这项能力。

扩展的核心承诺是：

> 用户与主对话之间的思考通道必须始终干净。用户只需要阅读主对话的理解、推理、结论和真正需要自己决定的问题，不需要观看工具调用、工具结果、运行日志和内部维护过程。

能力建设顺序固定为：

1. 普通 Pi 对话可选启用的 Idea 保持；
2. 面向数周或数月任务的上下文组装与无感压缩；
3. 由主模型调度的廉价 Worker 与 Workflow；
4. 不影响前三项的 CLI 体验增强。

后续能力不得反过来增加上下文漂移、污染主对话，或把扩展变成独立产品。

## 二、不可妥协的最终效果

### 1. 插件默认无感，任意对话可成长为长期对话

- 安装插件后，所有新对话仍然首先是普通 Pi 对话。
- 未启用 Idea 保持的对话，其启动、工具、session、模型、登录、压缩、快捷键和界面行为与原生 Pi 一致。
- 用户可以在对话开始时启用，也可以在已经进行很久的普通对话中启用。
- 启用 Idea 保持不会创建专门的 Idea 对话、第二个窗口或替代 session，也不会要求用户复制已有上下文。
- 每个对话独立决定是否使用 Idea 保持；一个对话的启用、暂停或关闭不能改变其他对话。
- 暂停或关闭 Idea 保持不能删除已有对话、Idea 历史或产物；重新启用后仍能继续。
- 用户不需要在开始对话时判断它是否会成为长期工作；任何对话都保留以后升级为长期工作的可能。
- 扩展即使损坏，也不能导致普通 Pi 无法启动或改变行为。

### 2. 启用长期保持后的主对话必须干净

主对话长期只允许出现三类内容：

1. 用户消息；
2. 主对话面向用户的自然语言回复；
3. 必须由用户处理的方向选择、Idea 修改确认、授权请求或不可恢复错误。

以下内容默认不得出现在主对话中：

- 工具名称、工具参数、工具调用卡片和工具结果；
- `read`、`grep`、`bash`、`edit`、`write` 等执行细节；
- stdout、stderr、测试日志、实验日志和完整 diff；
- Worker 或辅助模型的中间对话、中间推理和尝试过程；
- 重试、排队、索引、检索、摘要、压缩和上下文维护过程；
- Context Manifest、token 统计、Bank、内部版本号和刷新信息；
- 对用户下一步判断没有价值的成功通知。

工作期间可以有短暂状态提示，但它不能进入会话历史、不能推动终端滚屏、不能遮挡输入，也不能要求用户持续关注。任务完成后，状态提示应自动消失或变成一句有意义的最终结果。

### 3. 脏活累活不可丢失，但必须离开主对话

- 每一次后台工作都必须可追溯到发起它的用户请求或主对话决定。
- 用户主动查看时，能够知道做过什么、使用了什么证据、产生了什么文件、哪些步骤失败、哪些结果被采用或舍弃。
- 大体量原始日志可以按需查看，但不得自动复制进主对话或主模型上下文。
- 查看历史过程不能触发新的模型调用、工具调用或 Workflow。
- 过程记录的查看形式暂不规定；只要求它独立于主对话且随时可检查。

### 4. 主对话拥有方向，辅助工作没有方向权

- 主对话负责理解 Scientific Idea、科研推理、与用户讨论、决定当前路线和选择下一步。
- 辅助工作只能完成被交付的任务并返回证据或结构化结果。
- 辅助工作不能自动修改 Scientific Idea、终点标准、当前路线或阶段目标。
- 辅助结果与当前路线冲突时，主对话必须明确暴露冲突，而不是静默改写目标。
- 只有出现否决当前路线的关键反面证据，或需要改变科学对象与终点标准时，才必须让用户回来共同决定方向。

### 5. Idea 在长期工作中不漂移

Idea 至少承载：科学对象、终点标准和当前路线。

- 用户确认的核心 Idea 必须逐字保存。
- 对话变长、上下文压缩、退出恢复、session 分支、后台任务和程序升级都不能改写核心 Idea。
- 讨论、模型建议、实验结果、摘要和检索结果不能自动升级为权威 Idea。
- 修改 Idea 时，用户必须看见修改前后差异并明确确认。
- 用户拒绝或未确认的修改不能影响后续主对话。
- 任意时刻都能查看当前权威 Idea、上次确认时间和历史版本。
- 当前对话启用 Idea 保持后，Idea 的权威修改仍只来自用户在该对话中的明确确认。
- 其他对话不能因为安装了插件而自动读取、接管、共享或修改当前对话的 Idea；任何跨对话关联都必须由用户明确发起。

### 6. 上下文组装必须服务 Idea，而不是替代 Idea

- 每次用户消息触发主模型调用，以及同一轮中的每次后续 Agent loop，都必须准确知道当前核心 Idea、当前阶段目标和本步任务。
- 用户确认的核心 Idea 必须逐字位于实际模型输入的受保护前部；主模型和经授权的昂贵审查模型都完整携带它。
- 核心 Idea 不参与摘要、压缩或近似检索，不能因节省 token 被改写或省略。
- 其余历史信息按可独立替换的内容块管理。已失去当前用途的旧过程、工具细节和重复讨论可以被摘要或移出输入；仍影响结论的证据、已确认决策、失败教训和未解决冲突必须保留其可恢复含义。
- 每次实际调用都只携带本步需要的信息。不能因为信息曾经出现过，就永久占据以后所有调用的上下文。
- 用户能够检查本轮主对话实际依据了哪些信息、各自来源和状态，以及哪些信息没有被带入。
- 新证据出现、任务切换、阶段变化或即将压缩时，系统必须重新判断上下文；没有实质变化时不能反复重做相同工作。
- 上下文维护必须在达到模型极限之前渐进完成。用户发送消息后不得因为刚刚触发整段历史压缩而长时间等待，系统也不得等到请求失败或上下文溢出才开始处理。
- 摘要必须替代其覆盖的旧内容，而不是与旧内容和更早摘要永久并存。摘要层数、数量、总 token、存储、CPU 时间和模型调用次数都必须有界。
- 同一份没有变化的内容不得被循环摘要、递归检索或反复注入；一次维护事件不得触发自身形成无限调用链。
- 原始工具日志、完整实验输出和可重建细节默认留在主模型输入之外；只有当前判断需要的证据片段或结构化结论才能被选入。
- 受保护的核心 Idea 与阶段上下文合计不超过有效输入预算的 5%；超过时必须显式提示复盘，不能静默截断。

上下文方案采用严格的字典序优化，不使用会掩盖正确率损失的加权总分：

1. **首先最大化任务正确率。** 任何导致任务成功率明显下降的省 token、压缩或加速方案都直接淘汰。
2. **仅在正确率统计等价时最小化注入上下文。** 不能凭点估计相近就宣称等价；必须报告样本量、配对差异与置信区间。
3. **仅在前两项相当时最小化组装延迟。** 同时报告中位数与 P95，不能用平均值掩盖长尾等待。

Luna 增强方案与纯本地方案必须在同一公开任务基准、相同回答模型和相同评测规则下配对比较。纯本地方案是 Luna 不可用时的即时回退，不得阻塞主 Agent；其任务成功率相对 Luna 的差值必须通过 **10 个百分点非劣界值**，否则不得进入生产路径。

### 7. Workflow 必须减少打扰，而不是制造仪式

- 用户可以直接用自然语言推进工作，不需要先学习固定流程或填写大量字段。
- 多步骤任务可以持续运行、暂停、恢复和取消。
- 并行任务必须是添加关系，不能覆盖已有任务、Idea 或其他结果。
- 多个任务可以共享已经确认的背景和证据，但一个任务的临时假设不能自动污染其他任务。
- 用户在主对话中只看到阶段性结论、重要进展、冲突和最终结果。
- 主模型是控制者：它负责拆分任务、决定是否调用 Worker、评估返回结果并继续与用户讨论。
- 廉价 Worker 是主模型手中的工具，而不是并列的科研对话。它主要承担检索、整理、格式转换、批量检查、状态维护和其他边界清楚的简单任务。
- Worker 默认只获得完成当前任务所需的最小上下文，不继承整段主对话，也不承担核心科研推理、路线选择或复杂开发决策。
- Worker 只向主模型返回当前需要的证据、产物引用和结构化结果；中间对话、尝试过程与大体量输出不得注入主对话。
- 对于需要连续多轮工具操作的简单工作，主模型可以一次性交出一段 Agent loop 的执行权，而不是每完成一步都重新调用昂贵主模型轮询。
- 这段接管必须有明确任务边界、最小上下文、工具权限、预算、完成标准、停止条件和升级条件。廉价模型只能在该范围内连续工作，不能扩展科学目标或自行领取下一项任务。
- 接管结束时，廉价模型必须一次性归还结果摘要、证据与产物引用、已验证事项、失败事项、未解决风险和必要的上下文增量；强模型据此恢复控制并完成科研判断或面向用户的回复。
- 遇到目标歧义、关键证据冲突、可能改变路线、超出权限、连续失败、预算临界或无法验证完成时，廉价模型必须提前归还控制，而不是猜测、无限重试或继续消耗。
- 模型切换应以稳定工作段为单位，不能因一次普通工具错误而反射式来回切换；切换本身的上下文重建、缓存损失、延迟和 token 成本也必须计入是否值得切换的判断。
- 当简单任务适合安全并行且能显著减少昂贵模型等待或 token 时，主模型应能并行调用多个 Worker；并行结果只能追加，不能相互覆盖。
- 需要隔离上下文进行复杂研讨或审查时，可以调用昂贵模型线程，但必须处于用户当前阶段授权范围内；它仍然没有修改 Idea 的权限。
- 多次局部调整没有产生阶段成果时，系统应提醒复盘；提醒不能自动停止工作或替用户改变路线。
- 强制步骤只允许用于防止方向漂移、不可恢复错误或用户明确要求的质量门槛。

### 8. 成本与模型使用对用户可控

- 用户能知道一次任务是否使用了额外模型、昂贵模型或并行工作。
- 便宜能力足以完成的整理、检索和机械任务，应优先交给廉价 Worker，使昂贵主模型集中于理解、科研推理、冲突判断与任务控制。
- 可以多使用廉价模型 token，只要能够显著减少昂贵模型 token、缩短等待时间，并且不会降低证据质量或引入方向漂移。
- 需要额外昂贵审查时，必须符合用户当前阶段授权或获得即时授权。
- 节省 token 不能以丢失核心 Idea、关键证据或推理质量为代价。
- 成本信息按需可查，但默认不能占据主对话或持续刷新界面。

### 9. 中断、失败和恢复必须平静

- 任意后台任务失败，都不能破坏主对话、Idea、session 或已完成结果。
- 主对话只呈现与用户有关的失败结论：失败了什么、影响什么、是否需要决定。
- 原始堆栈和完整错误日志不进入主对话，但可在外部记录中查看。
- 用户可以取消正在进行的工作，取消后不能留下递归任务、持续重试或隐藏进程。
- 程序崩溃或电脑重启后，用户能够恢复主对话和未完成工作的真实状态；已经完成的操作不能被自动重复执行。
- 任何故障都不能造成 CPU、内存、磁盘或 token 消耗持续无界增长。

### 10. 长期科研使用必须自然

- 一个 Idea 可以持续数周或数月，而不要求用户定期重新解释核心目标。
- 用户离开后回来，能迅速知道上次确认了什么、目前卡在哪里、出现了什么新证据。
- 回顾必须简洁，不重放全部过程。
- 用户可以从总体思路进入具体证据，也可以从一个结果追溯到产生它的任务，但不被迫采用固定布局。
- 扩展不能让 Pi 因为长期积累而逐渐变慢、越来越占内存或不断扩大存储。

## 三、用户体验验收场景

### 场景 A：安装插件但不启用

1. 安装插件后启动普通 Pi。
2. 新建、恢复和分支原生 session。
3. 使用原生工具和快捷键。

通过标准：行为与未安装该扩展的 Pi 一致；未启用的对话中没有 Idea 状态、上下文注入或后台维护。

### 场景 B：普通对话中途成长为长期对话

1. 先以普通 Pi 对话交流若干轮。
2. 在当前对话中启用 Idea 保持并确认核心目标。
3. 继续当前 session，退出后再恢复。
4. 暂停并重新启用 Idea 保持。

通过标准：全过程始终是同一段原生 Pi 对话，不创建或迁移到专门 Idea 对话；启用前的讨论仍然存在，恢复后核心 Idea 和工作进度连续。

### 场景 C：工具密集任务仍保持干净

1. 在 Idea 主对话中要求完成一个需要大量读取、搜索、编辑和测试的任务。
2. 后台至少产生 50 次工具执行和大体量日志。
3. 任务完成后查看主对话。

通过标准：主对话中没有任何工具调用卡片、工具参数、原始结果、日志或 Worker 对话；用户只看到自己的请求、主对话给出的必要沟通与最终结果。

### 场景 D：主动追溯过程

1. 完成场景 C 后，用户要求查看某个结论的证据来源和执行过程。
2. 查看具体工具、错误、产物和时间关系。
3. 返回主对话。

通过标准：过程完整可查；查看行为不向主对话追加大段内容，不触发新任务，返回后思路位置不丢失。

### 场景 E：长对话与多次压缩

1. 模拟至少 100 轮对话、10 次压缩、20 次退出恢复和多个 session 分支。
2. 中途执行并行任务和失败任务。
3. 每个阶段检查核心 Idea。

通过标准：核心 Idea 字节与哈希保持不变；主对话仍能准确说明科学对象、终点标准、当前路线、关键证据与冲突。

### 场景 F：Idea 修改

1. 模型建议改变路线，但用户没有要求修改 Idea。
2. 用户随后明确要求提出修改。
3. 用户分别测试拒绝、继续调整和确认。

通过标准：建议不会自动生效；只有用户确认的精确版本成为新 Idea；拒绝后后续调用仍使用旧版本。

### 场景 G：反面证据与路线冲突

1. 后台工作发现能够否决当前路线的证据。
2. 同时还有若干普通工程失败和可修复错误。

通过标准：普通错误由系统继续处理；否决路线的证据被清楚带回主对话，并要求用户共同决定下一条路线，系统不擅自转向。

### 场景 H：崩溃、取消与资源压力

1. 在任务运行中取消、强制退出、断网和重启。
2. 让单次工具产生超大输出，并让任务连续失败。
3. 长时间观察 CPU、内存、存储和后台进程。

通过标准：没有递归调用、无限重试、重复执行或持续资源增长；主对话和 Idea 可恢复；失败只留下有界记录。

### 场景 I：旧版彻底隔离

1. 让 V0 archive、旧数据库和旧 session 全部不可访问。
2. 启动并使用新版的全部基本能力。
3. 搜索新版运行期访问记录和依赖关系。

通过标准：新版正常工作，不读取、不导入、不调用旧源码或旧状态；旧版只作为人工事故资料存在。

### 场景 J：渐进上下文维护而非溢出后急救

1. 让同一对话持续产生大量讨论、工具输出、实验记录和反复修改。
2. 在上下文使用率远未达到硬上限时持续发送新消息，并记录每轮首 token 等待时间和实际输入组成。
3. 让若干旧过程块失去当前用途，再让其中一项重新变得相关。

通过标准：旧块在后台逐步被摘要或移出活动输入，发送新消息不会触发一次阻塞式全历史压缩；核心 Idea 始终逐字存在；重新相关的信息能够从来源恢复；摘要没有与被覆盖内容无限叠加。

### 场景 K：上下文维护资源有界

1. 模拟数月事件、数千个内容块、多轮摘要和频繁任务切换。
2. 重复触发没有新证据的相同消息与维护条件。
3. 长时间观察模型调用、CPU、内存、存储和索引规模。

通过标准：没有摘要套摘要的无界链、递归检索、自触发 loop 或重复注入；资源使用随有效信息而非原始历史总量无界增长；过期中间状态能够被替换或回收，事实源仍可追溯。

### 场景 L：廉价 Worker 为主模型卸载简单工作

1. 给主模型一个同时包含检索、批量整理、机械验证和科研判断的任务。
2. 允许廉价 Worker 并行处理边界明确的简单部分。
3. 检查各模型实际收到的上下文和主对话最终内容。

通过标准：主模型仍控制任务和科研判断，但不必亲自执行全部简单工作；Worker 只收到最小必要上下文，只返回证据、产物引用和结构化结果；Worker 的过程不进入主对话；额外昂贵模型未经授权不会被调用。

### 场景 M：廉价模型连续接管而非昂贵模型轮询

1. 由强模型定义一项需要至少 20 个 Agent loop 才能完成、但科研方向已经明确的机械工作。
2. 将该工作交给廉价模型连续执行，并在中途分别制造普通工具错误、目标歧义和路线级冲突。
3. 记录强模型调用次数、模型切换次数、实际上下文、缓存/token 开销和最终交还内容。

通过标准：普通执行期间不需要强模型逐步轮询；普通工具错误由廉价模型在预算内自行恢复；目标歧义和路线级冲突会立即触发交还；最终只用一份有来源的结构化结果恢复强模型；接管期间不能修改 Idea、扩大任务或形成无限 loop。

## 四、发布门槛

### Gate 0：未启用时与原生 Pi 等价

- 安装插件但未启用 Idea 保持时，普通 Pi 对话与未安装插件的基线一致；
- 一个对话的 Idea 保持状态不能泄漏到其他对话；
- 旧 V0 源码、状态和启动方式不参与新版运行。

### Gate 1：原对话无缝获得长期能力

- 先通过场景 B，证明普通对话可以原地启用、暂停、恢复长期保持；
- 再用场景 C 证明工具过程不会进入主对话；
- 未通过场景 B、C 前，不加入上下文优化或 Workflow 能力。

### Gate 2：外部可追溯

- 通过场景 D；
- 过程不可查或查看会污染主对话时，不进入下一阶段。

### Gate 3：Idea 长期稳定

- 通过场景 E、F、G；
- 核心 Idea 出现一次非用户确认的变化即判定失败。

### Gate 4：上下文组装与无感压缩

- 通过场景 J、K；
- 公共基准上的方案选择严格按“任务正确率 → 注入上下文量 → 组装延迟中位数/P95”依次判定；前一项未达到统计等价时，不得以后两项优势补偿；
- Luna 增强与纯本地回退采用配对非劣检验；本地相对 Luna 的任务成功率差值置信区间必须满足预先登记的 10 个百分点非劣界值；
- 若仍需等到接近溢出再整段压缩，或摘要、CPU、内存、存储、模型调用会无界增长，则不得加入 Worker 或 Workflow。

### Gate 5：廉价 Worker 与 Workflow

- 先通过场景 H，再通过场景 L、M；
- 真实使用一段时间后再决定是否增加更多并行、昂贵审查或复杂 Workflow。

每个 Gate 都只依据黑盒结果验收。任何实现只要达到结果即可；任何实现即使设计精巧，但污染主对话、改变普通 Pi 或不能长期保持 Idea，都应被淘汰。

## 五、明确不规定的事项

本文不规定：

- 是否分屏、使用面板、overlay、Web UI 或纯 CLI；
- 使用多少进程、线程、Agent、Worker 或队列；
- 使用何种数据库、文件格式、索引或记忆算法；
- 是否采用事件驱动、RPC、MCP、hook 或其他扩展机制；
- 如何实现上下文压缩、检索、摘要与缓存；
- 状态信息位于上方、下方、临时窗口还是独立程序；
- 具体快捷键、颜色、主题和视觉风格。

这些都必须在满足最终效果的前提下，通过最小原型和真实使用再决定。

## 六、外部体验参考

只借鉴体验结果，不复制实现：

- Claude Code 将 task list 与 background tasks 分开，并允许按需切换任务视图：<https://code.claude.com/docs/en/interactive-mode>
- Claude Code 的 `/btw` 内容可见但不进入主 conversation history，说明辅助查看不必污染主对话：<https://code.claude.com/docs/en/interactive-mode>
- Claude Code Subagent 使用隔离上下文并向主对话返回总结结果：<https://code.claude.com/docs/en/sub-agents>
- Claude Code 返回终端时提供一行 session recap，而不是重放全部过程：<https://code.claude.com/docs/en/interactive-mode>
- Claude Code 官方公开仓库提供插件与 Workflow 示例，但不提供完整 CLI 主实现：<https://github.com/anthropics/claude-code>
- `pi-cc-extensions` 提供 Claude Code 风格折叠、工具摘要和上下文查看，源码为 MIT；其“仍显示工具卡片”的体验边界不满足本规格，因此不安装、不复制、不复用：<https://github.com/minuque/pi-cc-extensions>
