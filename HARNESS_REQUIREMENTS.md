# Personal Research Harness：核心需求 v0.5

> 状态：当前权威开发规格
> 日期：2026-08-14
> 基座：[`earendil-works/pi`](https://github.com/earendil-works/pi) 的原生扩展能力；V0 不 fork Pi 核心。

## 1. 产品定义

这是一个供个人科研长期使用的轻量 Harness。它仍像普通 Pi/Codex 一样以一个连续主对话为入口，但所有上下文组装、任务执行和历史恢复都服务于同一个版本化 Scientific Idea。

系统不替代研究者决定科学方向，也不把“改进 Harness、代码或工具”悄悄升级成研究目标。人和 AI 都是 Idea 的参与者；Idea 才是长期中心。

V0 只证明三件事：

1. 核心 Idea 不会因摘要、压缩、检索失败、退出恢复、分支或模型切换而漂移；
2. 每次模型调用都能获得可检查、可复现、受预算约束的上下文；
3. Idea 的修改只能在讨论后，经用户明确确认产生新版本。

## 2. Idea Space

一个 Idea 是独立于代码工作区的长期空间：

```text
Idea Space/
├─ IDEA.md                    # 当前权威 Idea Kernel，短且逐字保存
├─ .harness/
│  ├─ state.sqlite           # 版本、事件、提案、上下文清单、控制权
│  └─ P1.md                  # 兼容旧版的阶段视图，可为空
├─ evidence/
└─ artifacts/
```

- Idea 上下文只保存在 Idea Space，不跟随某个代码仓库。
- 一个 Idea 可绑定多个本地仓库、数据集或 SSH 环境；Workspace Binding 只定义操作路径与权限。
- 工作区结果只有被明确提升为带来源的 Idea Evidence 后，才可进入 Idea 上下文。
- 外部工作区的 `AGENTS.md` 只是执行策略，不能覆盖本规格的 Idea 不变量。

## 3. Idea Kernel / Research Frame / Working State

`IDEA.md` 保存用户确认后的简洁 Idea Kernel。它只承载长期稳定的科学对象、成功标准和不可被局部工程替代的边界，不承载当前路线。初始化时用户直接用自然语言描述想法，AI 只做忠实整理；涉及方向的歧义必须返回讨论，不能自行补全。候选只有经用户确认后才成为权威 Kernel。

当前路线放在独立的 Research Frame 中。模型可以提出精确 diff，但只有用户确认才能升级为新的权威 Frame。Working State 保存当前假设、证据缺口、下一动作和停止建议；模型可填充这些槽位，但不能改变 Kernel、确认 Frame 或宣告科学目标成立。

下面只是 Kernel 的可选写法，不是解析格式：

```text
科学对象：
<研究服务的对象>
终点标准：
<怎样才算科学目标达成>
不可替代边界：
<什么局部工程成果不能冒充科学目标达成>
```

Kernel 与 Frame 分别版本化。同一版本内确认文本按字节不可变。每次主对话和授权 Sol 调用都把 Kernel 放在输入消息最前方；它不依赖检索命中，也不进入 rolling summary。

Kernel 不放文献综述、实验历史、详细推导、当前路线、计划、TODO、线程状态或防御性长解释。

## 4. 兼容阶段视图

旧版 P1 继续作为短阶段视图读取；新状态优先进入 Working State，并保留来源与修订号。

阶段视图不采用强制卡片或固定科研流程。问题解决后移出；尚未解决的冲突不能因压缩消失。

## 5. 上下文预算与编译

设：

```text
B = 模型上下文窗口
    - 输出预留
    - 系统提示与工具定义估计
    - 安全余量
```

硬约束：

```text
Kernel + Frame + Working State + 兼容阶段视图 <= 5% × B
Kernel <= min(1200 tokens, 2% × B)
```

Kernel 目标长度不超过约 600 tokens，1200 是硬上限。1/20 是天花板，不是填满目标。中文采用保守 token 估计，避免 `字符数/4` 对 CJK 的系统性低估。

上下文优先级：

```text
Idea Kernel 逐字保留
Research Frame 用户确认后逐字保留
Working State 受权限约束
P2 当前任务证据
P3 相关历史
P4 可选启发
```

预算不足时依次删除或压缩 P4、P3、P2；绝不改写 Kernel 或 Frame。若权威层本身无法装入，停止调用并要求复盘，不得静默截断。

编译器只在阶段开始、任务切换、新证据出现、即将压缩等实质事件上重选动态内容；其他调用复用既有 Context Packet 并做增量更新。每次实际调用仍保存 Manifest：版本、来源、token 分配、排除原因、内容哈希和 Packet ID。

## 6. 主对话、线程与 Workflow

- 每个 Idea 同一时间只有一个活跃主对话控制者；新窗口可恢复或显式接管，但不能并列修改方向。
- Pi 原生多会话、树、fork、resume 全部保留。并行工作只能追加结果，不能覆盖规范状态。
- Luna 工作线程是主对话调用的短期工具，主要执行检索、整理、日志解析和简单任务；只返回当前需要的证据与结构化结果，不把完整线程历史灌回主对话。
- Sol 是隔离上下文的高成本审查/研讨线程。阶段授权或用户正在直接讨论并发起调用时可用，否则必须获得用户授权。主对话与 Sol 完整携带 P0；Luna 只携带任务切片、必要约束与路线版本。
- Workflow 默认读取已提交的共享快照与相关新证据增量；主对话可显式选择实时全局上下文模式。
- 多次局部假设调整仍无阶段成果时触发软检查点，进行复盘或建议 Sol 审查；之后可在硬预算内继续，硬上限需用户决定。

V0.1 已实现 Luna 的第一条纵向闭环：主对话在实质上下文事件上提交精确任务切片与必要约束；Luna 从带来源 ID 的会话历史中选择条目，返回摘要、相关理由和冲突；Harness 保存不可变派生快照，并以该快照替代生成时点之前的普通历史，只保留之后的增量消息。不存在的来源 ID、超条目数和超预算快照会被拒绝。Luna 不能修改 P0/P1、决定路线、执行复杂开发或提交 Idea。

V0.1 尚不实现 Luna 简单任务工作线程、Sol 和 Workflow 调度，但数据与上下文边界不得阻碍后续加入。

## 7. Idea 修改：提案而不是直接编辑

主对话可在用户说“修改 Idea”后生成 `IdeaChangeProposal`。创建或调整提案都不会写入权威 `IDEA.md`。

```text
讨论完成
  → 主对话生成候选版本
  → 二级界面显示精确 diff
  → [确认修改]
     [回主对话继续调整]
     [手动调整提案]
     [放弃]
```

- `确认修改` 是唯一提交入口；提交后创建新的不可变 Idea 版本并保存父版本、哈希、作者、原因和证据引用。
- `回主对话继续调整` 只把待调整提示放回输入框，不改变权威状态。
- `手动调整提案` 修改候选内容并生成新的提案 revision，仍需再次确认。
- `放弃` 保留审计记录但不改变 Idea。
- 科学对象和终点标准的任何变化都必须由用户确认。路线内的小调整也先形成提案；路线被反证或要换路线时，由用户与主对话共同决定下一条路线。
- 已运行 Workflow 保留启动时捕获的旧 Idea 快照，并明确标注版本，不被新版本回写。

## 8. 界面

主界面保持 Pi 的单栏对话体验，只在输入框上方增加一条轻量入口（同时保留短状态）：

```text
Idea v3 · 路线 v2 · Context 3.1k · Luna 1 · 冲突 1 · 待确认 1
```

按需打开二级界面查看 Idea、版本 diff、实际注入上下文、证据、冲突和任务进度。V0 的 CLI/TUI 操作菜单等价于按钮；未来富前端可直接映射为可点击按钮，不改变状态机。

最小命令入口：`/idea`、`/context`、`/luna`、`/think`，以及初始化、返回主对话和显式接管入口。`/luna` 展示当前快照、来源、冲突和 Context Diff，并允许用户刷新或停用。`/think` 与 `Alt+T` 可直接选择思考等级，Pi 原生 `Shift+Tab` 可循环切换。普通聊天不被仪表盘或固定侧栏打断。

## 9. 存储与索引边界

- `IDEA.md` 是人可读权威视图；SQLite 保存不可变版本与追加事件。
- Context Packet、重入包、搜索索引和摘要都是派生缓存，不是事实源。
- Obelisk 作为可捆绑安装、可替换、可降级的历史证据 sidecar；它提供候选材料与来源 ID，但不能修改路线或决定注入内容。
- Research Skill 是按需触发的程序性工具箱，不保存 P0/P1、路线、实验结论、冲突、聊天或凭据。

## 10. V0.1 实现范围

本轮实现：

1. 可本地加载的 Pi package/extension；
2. 自然语言 Idea 初始化、AI 忠实整理候选、用户确认、SQLite 事件与不可变版本；
3. P0/P1 编译、1/20 预算、Manifest 与哈希；
4. 每次 Pi 模型调用前的固定前缀注入；
5. `IDEA.md` 直接写入拦截与完整性冲突检测；
6. Idea 提案、revision、diff、确认提交、继续调整和放弃；
7. 轻状态行、Idea/Context 二级面板；
8. 主会话标记与显式接管的最小控制权机制；
9. 不调用付费模型的确定性测试与 Pi 加载冒烟测试。
10. `gpt-5.6-luna` 隔离历史选择、来源约束、版本绑定、快照注入、旧历史替代与 Context Diff。

暂不实现：完整 Workflow 引擎、Luna 简单任务执行线程、Sol 运行时、远端 Workspace Binding、Obelisk UI、向量/图检索、实验监控面板和富 GUI。

## 11. V0 验收标准

- 连续十次模拟压缩、退出重载、会话分支后，P0 内容与哈希完全不变且仍位于每次消息上下文最前方。
- 动态历史为空或检索失败时，P0 仍确定性出现。
- P0/P1 超预算时调用被停止，内容不被自动摘要或截断。
- 模型创建或反复调整提案时，`IDEA.md` 与当前版本不变；只有用户确认后才新增版本。
- 用户能查看每次实际注入的 Packet ID、来源、预算和哈希。
- 普通 `write`、`edit` 和显式 shell 路径不能直接覆盖权威 Idea。
- Pi 原生聊天、压缩、resume、fork 和其他未绑定 Idea 的会话继续可用。
