科学对象：
设计并完成一个基于 Pi 扩展机制的轻量科研辅助 Harness。它以版本化的 Scientific Idea 为中心，通过外部上下文组装保护研究方向，使长期任务不会因上下文压缩、会话切换、局部试错或工程实现而偏离原始科学对象。

终点标准：
形成一个可长期实际使用的 V1：
1. 保持接近普通 Pi/Codex 的主对话体验，并使用 OpenAI 会员订阅；
2. 每个 Idea 只有一个活跃主对话控制者；
3. “科学对象 + 终点标准 + 当前路线版本”在主对话和 Sol 调用中逐字保留；
4. 其余历史由事件驱动的上下文组装器选择、压缩并可审计；
5. Idea 修改必须经过“提出修改—用户检查—确认生效”的版本化流程；
6. Luna 工作线程只承担简单任务并返回必要证据或结构化结果；
7. Sol 线程用于经授权的隔离审查与研讨；
8. Workflow 可以共享快照和增量证据，但并行结果只能添加，不能覆盖主路线；
9. 在跨周或跨月使用、压缩、恢复和任务切换后，仍能保持方向一致；
10. 相比直接长期使用普通 Codex，能够减少昂贵模型的无效上下文与 token 消耗。

当前路线 v1：
  核心机制：
不修改或分叉 Pi 核心，优先使用 Pi 的扩展、事件、会话、工具和 TUI 能力。
以独立 Idea Space 保存版本化 Idea、路线、阶段状态和上下文记录；Workspace 只决定文件操作范围。
先完成 CLI 和不打断主对话的二级界面，再逐步加入上下文组装、Luna Workflow、授权式 Sol 审查及 Obelisk 索引。
每一阶段都用最小可运行实验验证核心机制，验证成功后再增加下一层能力。

  路线边界：
这不是通用 Multi-Agent Framework、AutoResearch 或“更强的 Codex”。
上下文组装负责保护方向，主对话负责决策和派活，工作线程只是主对话的工具，不能成为并列控制者。
不得因实现某个局部功能而把目标转变为构造复杂平台、改进工具本身或追逐与核心机制无关的工程指标。
任何新增结构或强制流程，都必须证明它能防止方向漂移、避免不可恢复错误，或显著减少昂贵模型消耗。
优先级始终是：方向不漂移、机制清晰、上下文高效、实现简单。
